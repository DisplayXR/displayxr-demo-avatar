build/
installer/*.exe
installer/macos/*.pkg
_artifact/
_package/
*.log

# Android (Gradle/NDK) — build artifacts + machine-specific SDK paths
local.properties
.gradle/
android/build/
android/.cxx/

# Android signing keystores — never commit (CI uses GitHub secrets)
*.jks
*.keystore
